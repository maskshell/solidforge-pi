---
description: "SolidForge arm-tools SMOKE TEST (M0.5) — verifies colon-namespace prompt-template command"
argument-hint: "[--with-tools]"
---
You are running a smoke test. The template `solidforge:arm-tools` expanded correctly — argument-hint works, $ARGUMENTS substitutes as: "$ARGUMENTS". Do NOT run any real arming. Reply with exactly this line and nothing else: TEMPLATE-OK args=[$ARGUMENTS]
