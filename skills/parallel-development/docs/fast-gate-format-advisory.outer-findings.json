{
  "findings": [
    {"defect_id": "novel-1", "severity": "warning", "kind": "gap", "location": "doc-split set vs hooks-reference.md:26", "evidence": "hooks-reference.md:26 documents the fast-gate as 'per-file cheap lint/format' — the prose the plan splits everywhere else; no plan item covered it.", "disposition": "fixed — hooks-reference-md-split item added (fg-7); conflict_rule notes rule 5 wins over the design §6 file list."},
    {"defect_id": "novel-2", "severity": "warning", "kind": "blind-spot", "location": "fast-gate-guidance-split.risk / DoD", "evidence": "smoke_gates.py contains ZERO fast-gate tests (grep 'fast' → no matches); the DoD was vacuous w.r.t. the changed code path.", "disposition": "fixed — behavioral test added to fg-1 DoD (lint-failing + format-failing fixtures, asserts split guidance, graceful skip); false risk claim corrected."},
    {"defect_id": "novel-3", "severity": "warning", "kind": "contradiction", "location": "commit-stratification-doc + ADR scopes vs plan-driven-mode.md:192", "evidence": "'one→two commits at the same point' holds only for C-post; C-pre commits style: BEFORE the logic edit (a new mid-stage commit point).", "disposition": "fixed — per-mechanism authorization framing in fg-2/fg-8 scopes AND the design doc's Honesty line (precision fix, non-restructIVE)."},
    {"defect_id": "novel-4", "severity": "warning", "kind": "blind-spot", "location": "commit-stratification-doc.scope", "evidence": "ADR #39 makes inline mode legitimate/default for sequential work; the design framed the actor as dispatched-Coder only.", "disposition": "fixed — fg-2 scope requires the protocol doc to state the inline-mode execution path (orchestrator executes C-pre mid-loop; ADR #39 bookkeeping still driven)."},
    {"defect_id": "novel-5", "severity": "warning", "kind": "gap", "location": "maturity-md-caveats.scope", "evidence": "Design §6's Option-A coverage-gap clause (Java/Go/Rust lose their ONLY fast-gate check under A) was silently dropped from the item scope.", "disposition": "fixed — clause added to fg-6 scope."},
    {"defect_id": "novel-6", "severity": "warning", "kind": "blind-spot", "location": "fg-1/ADR item-level trace of the /tmp gap note", "evidence": "Plan §6's 'do not silently bundle the /tmp lint gap' mandate had no item-level trace; an agent executing from the executable subset got no signal.", "disposition": "fixed — explicit note in fg-1 risk + fg-8 (ADR) scope: the gap stays OPEN, out of this change."}
  ],
  "reviewer_summary": "Dependency edges, DAG, terminology, the self-gate list, Option-A/B out-of-scope discipline, and the Option-C no-demotion invariant all check out faithful. Six warnings, zero blockers.",
  "orchestrator_dispositions_note": "ALL six warnings ACCEPTED and fixed in the plan-model + plan.md (and one precision fix in the design doc's Honesty line, novel-3, non-restructIVE). No blocker-severity findings."
}
