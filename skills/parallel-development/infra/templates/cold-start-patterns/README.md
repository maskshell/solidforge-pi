# Cold-Start Patterns (Warning-tier Few-Shot)

Used only when a golden-path search (`@Agent-Golden-Ref`) returns zero hits. These are generic, standardized design-pattern templates — Warning tier, NOT Blockers. They guide naming and style; they are not authoritative for this project. When a real golden path exists, it always wins over these.

Each file is a self-contained pattern slice an agent can mirror.
